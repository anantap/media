# strangelog

A private, local-only microblog for posting absurdist one-liners to yourself. No backend, no login, no analytics — everything lives in your browser's localStorage.

```bash
npm install
npm run dev
```

Deploy: push this folder to a Git repo and import it on [Vercel](https://vercel.com/new) — it auto-detects Vite, zero config needed.
